#!/usr/bin/env node

var program = require('commander')

program
  .version('1.3.0')
  .description('Tool for examining and setting text chunks in PNG images')
  .command('get [options]', 'search for text chunks')
  .command('set [options]', 'add or set text chunks')
  .parse(process.argv)
